CHIP test
{
	IN	in[4],in2[4];
	OUT out[4];
	
	PARTS:
	
	
	//And(a=in,b=in2,out=out); ---> this wont work, a is a single bit
	And(a=in[0],a=in[1],a=in[2],a=in[3],b=in2[0],b=in2[1],b=in2[2],b=in2[3],out=out[0],out=out[1],out=out[2],out=out[3]);
}